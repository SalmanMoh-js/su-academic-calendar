import { StyleSheet } from 'react-native';
import tw from 'twrnc';

export const styles = StyleSheet.create({
  menuBtn: {
    ...tw.style('w-4/5 text-center h-16 m-auto bg-blue-400'),
  },
});
